"""LatentSpec CLI (`latentspec`).

Week-2 deliverable: `latentspec check` runs a regression check against an
agent's active invariant set. The CLI is the foundation that the GitHub
Action wraps so local and CI code paths are identical.
"""
